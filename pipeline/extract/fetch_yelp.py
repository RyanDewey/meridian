import requests
import pandas as pd

yelp_base_url = 'yelp.com/data/'

def get_business_data(zip_codes):
    # Get business data from yelp
    #response = requests.get(f"{yelp_base_url}")

    # Store in dataframe
    #df = pd.DataFrame(response)

    # Return dataframe
    #return df

    pass